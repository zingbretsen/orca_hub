# Database (defaults match docker-compose.yml)
DB_USERNAME=orca_hub
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_NAME=orca_hub_dev

# OpenAI API key — used for auto-generating session titles
# OPENAI_API_KEY=sk-...

# Port (default: 4000)
# PORT=4000
